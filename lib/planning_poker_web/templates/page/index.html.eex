<%= if @maybe_user == nil do %>
    <span><%= link "Login with Google", to: auth_path(@conn, :request, "google") %></span>
<% else %>
    <h3>Hi <%= @maybe_user.email %>!</h3>
    <span><%= link "Logout", to: page_path(@conn, :logout), method: :post %></span>
<% end %>

